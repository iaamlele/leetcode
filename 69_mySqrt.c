#include <stdio.h>

int mySqrt(int x) {
    int flag = 0;
    for(int i = 0; i < (x / 2); i++) {
        if()
    }
}

int main() {
    int res = mySqrt(8);
    printf("res:%d\n", res);
    return 0;
}